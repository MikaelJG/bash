
if ! which "$1"; then
    # if argument given can't be found with which. 
    echo "it is not installed"
fi


if ! which "$1"; then
    # if argument given can't be found with which. 
    echo "it is not installed"
fi

